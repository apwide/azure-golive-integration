{
  "id": "{{taskid}}",
  "name": "send-deployment-infos{{tag}}",
  "friendlyName": "(Golive{{tag}}) Send deployment information",
  "description": "Send deployment information to Apwide Golive for Jira",
  "author": "apwide",
  "helpMarkDown": "Replace with Markdown to show in help",
  "category": "Build",
  "visibility": [
    "Build",
    "Release"
  ],
  "demands": [],
  "version": {
    "Major": "{{major}}",
    "Minor": "{{minor}}",
    "Patch": "{{patch}}"
  },
  "minimumAgentVersion": "1.95.0",
  "instanceNameFormat": "Service Endpoint Build Task $(project)",
  "inputs": [
    {
      "name": "serviceConnection",
      "type": "connectedService:golive",
      "label": "Service Connection",
      "defaultValue": "",
      "required": true,
      "helpMarkDown": "Select the Apwide Golive for Jira end point to use. If needed,selection 'manage', and add a new service endpoint of type 'Apwide Golive server connection'"
    },
    {
      "name": "applicationId",
      "type": "pickList",
      "label": "Application",
      "required": false,
      "helpMarkDown": "Select the name of the Apwide Golive for Jira application.",
      "properties": {
        "EditableOptions": "True"
      },
      "visibleRule": "serviceConnection > 0"
    },
    {
      "name": "environmentId",
      "type": "pickList",
      "label": "Environment",
      "required": true,
      "helpMarkDown": "Select the name of the Apwide Golive for Jira environment.",
      "properties": {
        "EditableOptions": "True"
      },
      "visibleRule": "applicationId > 0"
    },
    {
      "name": "versionName",
      "type": "string",
      "label": "Version Name",
      "required": true,
      "helpMarkDown": "Name of the deployed version.",
      "visibleRule": "environmentId > 0"
    },
    {
      "name": "buildNumber",
      "type": "string",
      "label": "Build Number",
      "required": false,
      "defaultValue": "$(Build.BuildNumber)",
      "helpMarkDown": "Build number.",
      "visibleRule": "environmentId > 0"
    },
    {
      "name": "description",
      "type": "multiline",
      "label": "Description",
      "required": false,
      "defaultValue": "<b>✅ Job #$(Build.BuildId) - $(Build.DefinitionName)</b>\nRequested by: $(Build.RequestedFor) \nBranch: $(Build.SourceBranchName)",
      "helpMarkDown": "Description of the deployment.",
      "properties": {
        "resizable": "true",
        "rows": "10",
        "maxLength": "2000"
      },
      "visibleRule": "environmentId > 0"
    },
    {
      "name": "issueKeys",
      "type": "string",
      "label": "Jira Issue Keys",
      "required": false,
      "helpMarkDown": "Comma separated list of Jira Issue Keys that are deployed. Ex: ECOM-3412,ECOM-6783,PAY-98",
      "visibleRule": "environmentId > 0"
    }
  ],
  "dataSourceBindings": [
    {
      "target": "applicationId",
      "endpointId": "$(serviceConnection)",
      "dataSourceName": "applications",
      "resultTemplate": "{ \"Value\" : \"{{{id}}}\", \"DisplayValue\":\"{{{name}}}\"}"
    },
    {
      "target": "environmentId",
      "endpointId": "$(serviceConnection)",
      "dataSourceName": "environments",
      "parameters": {
        "applicationId": "$(applicationId)"
      },
      "resultTemplate": "{ \"Value\" : \"{{{id}}}\", \"DisplayValue\":\"{{{name}}}\"}"
    }
  ],
  "execution": {
    "Node": {
      "target": "main.js",
      "argumentFormat": ""
    },
    "PowerShell3": {
      "target": "sample.ps1"
    }
  }
}
