{
  "scripts": {
    "build": "tsc && npm run lint && npm run test && webpack --config=webpack.config.js --env ${BUILD_ENV}",
    "build:package": "npm run build && npm run package",
    "clean": "rimraf ./dist && rimraf ./build",
    "clean:build": "npm run clean && npm run build",
    "lint": "npm run lint:eslint && npm run lint:format",
    "lint:eslint": "eslint -c .eslintrc.js --ext .ts src",
    "lint:fix": "npm run lint --fix",
    "lint:format": "prettier --check src",
    "fix:format": "prettier --write src",
    "test": "mocha",
    "start": "node --no-deprecation dist/environment-info/main.js",
    "create:task": "node scripts/golive.js create:task",
    "do:create:task": "tfx build tasks create --task-id ${TASK_ID} --token ${ADO_PAT} --service-url ${ADO_ACCOUNT_URI} || true",
    "clean:task": "node scripts/golive.js clean:task",
    "do:clean:task": "tfx build tasks delete --task-id ${TASK_ID} --token ${ADO_PAT} --service-url ${ADO_ACCOUNT_URI} || true",
    "package": "tfx extension create --root dist --manifests base.json --output-path dist/${BUILD_ENV}.vsix --publisher ${DEV_PUBLISHER}",
    "publish": "tfx extension publish --vsix dist/${BUILD_ENV}.vsix --token ${ADO_PAT}",
    "dev": "npm run build && npm run package && npm run publish && npm run publish:environment-info-1 && npm run publish:environment-info-2 && npm run publish:release-info",
    "publish:environment-info-1": "tfx build tasks upload --task-path dist/EnvironmentInfo/EnvironmentInfoV1 --overwrite --token ${ADO_PAT} --service-url ${ADO_ACCOUNT_URI} ",
    "dev:environment-info-1": "npm run build && npm run publish:environment-info-1",
    "clean:publish:environment-info-1": "npm run clean:task && npm run publish:environment-info-1",
    "publish:environment-info-2": "tfx build tasks upload --task-path dist/EnvironmentInfo/EnvironmentInfoV2 --overwrite --token ${ADO_PAT} --service-url ${ADO_ACCOUNT_URI} ",
    "dev:environment-info-2": "npm run build && npm run publish:environment-info-2",
    "clean:publish:environment-info-2": "npm run clean:task && npm run publish:environment-info-2",
    "publish:release-info": "tfx build tasks upload --task-path dist/SendReleaseInfo/SendReleaseInfoV1 --overwrite --token ${ADO_PAT} --service-url ${ADO_ACCOUNT_URI} ",
    "dev:release-info": "npm run build && npm run publish:release-info",
    "clean:publish:release-info": "npm run clean:task && npm run publish:release-info"
  },
  "devDependencies": {
    "@types/mocha": "10.0.6",
    "@types/node": "20.11.15",
    "@types/q": "1.5.8",
    "@typescript-eslint/eslint-plugin": "6.20.0",
    "@typescript-eslint/parser": "6.20.0",
    "clean-webpack-plugin": "4.0.0",
    "copy-webpack-plugin": "12.0.2",
    "eslint": "8.56.0",
    "eslint-config-prettier": "9.0.0",
    "eslint-plugin-import": "2.29.0",
    "eslint-plugin-prefer-arrow": "1.2.3",
    "mocha": "10.2.0",
    "prettier": "3.2.4",
    "replace-in-file-webpack-plugin": "1.0.6",
    "rimraf": "5.0.5",
    "string-replace-loader": "3.1.0",
    "tfx-cli": "0.16.0",
    "ts-loader": "9.5.1",
    "ts-node": "10.9.2",
    "tslint-config-prettier": "1.18.0",
    "typescript": "5.3.3",
    "webpack": "5.90.0",
    "webpack-cli": "5.1.4"
  },
  "name": "azure-pipelines-custom-task",
  "private": true,
  "version": "0.10.0",
  "dependencies": {
    "azure-devops-node-api": "12.4.0",
    "azure-pipelines-task-lib": "4.9.0",
    "node-fetch": "3.3.2"
  }
}
